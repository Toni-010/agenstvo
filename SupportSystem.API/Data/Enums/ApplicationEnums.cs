
namespace SupportSystem.API.Data.Enums 
{
    public enum UserRole { User, Manager, Admin }
    public enum OrderStatus { New, Processing, Completed, Cancelled }
    public enum RequestStatus { New, Processing, Completed, Cancelled }
    public enum Priority { Low, Medium, High }
}